#!/usr/bin/env node

const chalk = require("chalk");

const log = console.log;

log(chalk.yellow.bold(`Not Zaryab`));
log(chalk.gray.bold(`Github - https://github.com/zaryabdev`));
log(
    chalk.blue.bold(
        `LinkedIn: https://www.linkedin.com/in/talha-zaryab-0b0162213/`
    )
);
log(chalk.red.bold(`Email - zaraybdev404@gmail.com`));

// function isEven(num) {
//     log(chalk.blue.bold(num % 2 === 0));
// }

// function isOdd(num) {
//     log(chalk.red.bold(num % 2 !== 0));
// }

// module.exports = { isEven, isOdd };
